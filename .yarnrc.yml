enableScripts: true

nodeLinker: node-modules

# Yarn 4.17+ defaults to a 24h publish cooldown; disable so recently
# published deps (e.g. octane@0.1.12) resolve the same as under npm.
npmMinimalAgeGate: 0

yarnPath: .yarn/releases/yarn-4.17.1.cjs
