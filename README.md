# Prompt Composer

Desktop prompt composer for XLSX prompt archives. The UI is [Octane](https://octanejs.dev/); all lookup/composition runs in a Tauri Rust backend.

## Agent notes

See [AGENTS.md](./AGENTS.md) — Octane guidance: https://octanejs.dev/llms.txt

## Requirements

- Node.js 26.3.0 / Yarn 4.17.1 (Volta + Corepack pins in `package.json`)
- Rust toolchain with Tauri Linux deps (if on Linux/WSL)
- For e2e on headless Linux/WSL: `xvfb` (`xvfb-run`)

## Run

```bash
yarn install
yarn tauri dev
```

## Workflow

1. Launch the app (no spreadsheet is bundled).
2. **Upload archive** — pick an `.xlsx`, or **Create blank archive** to save a template with the correct sheets and `e.g.` hint rows.
3. Enter a freeform query and **Compose**.
4. Use **Replace** / **Clear** in the header chip to change or remove the stored archive.

Deterministic test fixture:

`fixtures/minimal_prompt_archive.xlsx`  
Golden query: `2 1lvl1 2lvl1 1lvl2` → `BODY_ALPHA OUTFIT_1_1 POSE_2_1 ACTION_1_2`

Regenerate fixture:

```bash
yarn fixture:gen
```

## Query language

```text
<excel-row> <level>lvl<index> [<level>lvl<index> ...]
```

Shorthand `N/M` is equivalent to `NlvlM` (e.g. `7 1/5 2/3 1/4` ≡ `7 1lvl5 2lvl3 1lvl4`).

Token order after the row: Outfit → Pose → Action → Scene (optional).

## Tests

```bash
yarn test                 # unit + integration + full e2e (incl. GUI)
yarn test:ci              # lint + unit + integration + Rust API e2e (CI)
yarn test:unit            # Rust lib tests + Vitest
yarn test:integration     # Rust archive lifecycle
yarn test:e2e             # Rust API e2e + GUI WebDriver
yarn fixture:gen          # Regenerate fixtures/minimal_prompt_archive.xlsx
```

### Fixture

Checked-in workbook: `fixtures/minimal_prompt_archive.xlsx` (generated by `src-tauri/src/bin/gen_fixture.rs` from shared constants in `fixtures_data.rs`).

| Sheet    | Keys (examples)                         | Prompt / body examples      |
|----------|-----------------------------------------|-----------------------------|
| Subjects | Excel rows 2–3                          | `BODY_ALPHA`, `BODY_BETA`   |
| Outfits  | L1-01, L2-02, L5-30                     | `OUTFIT_1_1`, …             |
| Poses    | L2-01, L1-01, L3-05                     | `POSE_2_1`, …               |
| Actions  | L1-02, L4-10, L2-03                     | `ACTION_1_2`, …             |
| Scenes   | L3-01, L1-01, L5-15                     | `SCENE_3_1`, …              |

Golden: `2 1lvl1 2lvl1 1lvl2` → `BODY_ALPHA OUTFIT_1_1 POSE_2_1 ACTION_1_2`  
Alt: `3 5lvl30 1lvl1 4lvl10` → `BODY_BETA OUTFIT_5_30 POSE_1_1 ACTION_4_10`

### CI & releases

- **Pull requests:** [`.github/workflows/ci.yml`](.github/workflows/ci.yml) runs `yarn test:ci` (fmt, clippy, unit, integration, API e2e).
- **Push to `main`:** [`.github/workflows/release.yml`](.github/workflows/release.yml) re-runs those checks, then builds installers and publishes a GitHub Release.
  - Tag: `v0.1.<run_number>` on the pushed commit (monotonic so upgrades work).
  - Artifacts: Linux `.deb` + AppImage, Windows NSIS `-setup.exe`, plus signed updater `.sig` files and `latest.json` (when signing secrets are set).
  - **In-app updates:** on startup the app checks `releases/latest/download/latest.json` and can install a newer **NSIS** or **AppImage** build after confirmation. `.deb` installs still upgrade via the package manager.
  - **Signing secrets (required for updater artifacts):** repo secrets `TAURI_SIGNING_PRIVATE_KEY` and `TAURI_SIGNING_PRIVATE_KEY_PASSWORD` (use empty password secret if the key has none). The matching public key is embedded in `src-tauri/tauri.conf.json`. If the private key is lost, already-installed apps cannot verify future updates. Prefer a single-line base64 secret (CI also strips newlines).
  - **Stable download assets:** each Windows release also uploads `version.json` and `Prompt.Composer_x64-setup.exe` for ELL-Toolkit masked downloads.
  - **Manual upgrade fallback:** product id is `com.promptcomposer.desktop` (renamed from `com.promptcomposer.app` to avoid the macOS `.app` suffix warning — reinstall once if upgrading from an older id). Re-run a newer NSIS setup or install a newer `.deb`; AppImage: replace the previous file.

### E2E notes

- Runs Rust `e2e_app_flow`, then `tauri build --debug --no-bundle`, then `e2e/run.mjs`. The GUI binary must come from `tauri build` last — a subsequent plain `cargo build` / `cargo test` can drop embedded frontend assets.
- GUI runner talks to embedded WebDriver (`tauri-plugin-wdio-webdriver`) via raw `fetch` (WDIO’s HTTP client hits `UND_ERR_INVALID_ARG` against this server).
- Sets `PROMPT_COMPOSER_E2E=1` so `import_archive_from_path` / `e2e_reload_frontend` are allowed.
- Under E2E, Rust natively navigates to `tauri://localhost` (WebKit otherwise stays on `about:blank`); reloads use `e2e_reload_frontend`.
- Uses a random `TAURI_WEBDRIVER_PORT` (or the env override) and isolates data under `.e2e-data/` (`XDG_DATA_HOME`).
- On headless Linux/WSL: `xvfb-run -a yarn test:e2e` if you have no display.
